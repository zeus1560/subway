# 제출 안내

## 제출 전 확인사항

1. ✅ 불필요한 문서 파일 제거 완료
2. ✅ README.md 간단하게 정리 완료
3. ✅ node_modules, .next 등 빌드 파일 제외

## ZIP 파일 생성 방법

### Windows 사용자

PowerShell에서 다음 명령어 실행:

```powershell
cd station1
.\create-submission-zip.ps1
```

또는 수동으로:

1. `station1` 폴더 선택
2. 다음 폴더/파일 제외:
   - `node_modules`
   - `.next`
   - `.git`
   - `.env.local`
   - `*.log`
   - `coverage`
3. ZIP 파일로 압축

### Linux/macOS 사용자

터미널에서 다음 명령어 실행:

```bash
cd station1
chmod +x create-submission-zip.sh
./create-submission-zip.sh
```

## 제출 파일 구성

제출 ZIP 파일에는 다음이 포함됩니다:

- ✅ 소스 코드 (app/, components/, lib/ 등)
- ✅ 설정 파일 (package.json, tsconfig.json 등)
- ✅ 문서 (README.md, AI_USAGE_GUIDE.md)
- ✅ 데이터 파일 (subway_passengers.csv)
- ❌ node_modules (제외)
- ❌ .next 빌드 폴더 (제외)
- ❌ .env.local 환경 변수 파일 (제외)

## 제출 시 포함할 내용

1. **ZIP 파일**: `station1-submission-YYYYMMDD.zip`
2. **설명 문서** (선택사항):
   - 프로젝트 개요
   - 주요 기능 설명
   - 실행 방법

## 주의사항

- `.env.local` 파일은 제출하지 마세요 (개인 API 키 포함)
- `node_modules`는 제출하지 마세요 (너무 큼)
- 빌드 파일 (`.next`)은 제출하지 마세요

